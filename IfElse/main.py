"""
if kondisi:
	perintah yang akan dijalankan

if kondisi:
	perintah yang akan dijalankan
else:
	perintah yang akan dijalankan

if kondisi:
	perintah yang akan dijalankan
elif perintah yang akan dijalankan:
	perintah yang akan dijalankan

"""